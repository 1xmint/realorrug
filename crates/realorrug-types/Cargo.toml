[package]
name = "realorrug-types"
description = "Domain vocabulary for Radar. Types only — no I/O, no logic that can fail at runtime."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true

[dependencies]
bs58.workspace = true
schemars.workspace = true
serde.workspace = true
thiserror.workspace = true
# `ChainAddress` wraps this chain's own address type beside Solana's. Robinhood
# holds no key and reads nothing at runtime, so depending on it here does not
# widen what this crate can do (still no I/O), only what it can name.
realorrug-robinhood.workspace = true

[dev-dependencies]
serde_json.workspace = true

[lints]
workspace = true
